# generated deployment records (addresses are public on-chain; keep the repo clean)
deployments.*.json
playground.*.json

# Local throwaway test wallets (private keys) — NEVER commit
test-wallets.json
